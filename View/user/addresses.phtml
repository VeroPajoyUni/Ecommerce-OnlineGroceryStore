<?php
/**
 * Vista Direcciones — AnimaMarket
 * Ruta: View/user/addresses.phtml
 */
?>

<div class="page-enter">

    <div class="d-flex align-items-center gap-3 mb-4">
        <button class="btn-back" onclick="history.back()" title="Volver">
            <img src="<?= BASE_URL ?>icons/volver.png" alt="Volver" class="btn-back-icon">
        </button>
        <div class="d-flex justify-content-between align-items-center flex-grow-1">
            <h2 class="section-title mb-0">
                <i class="bi bi-geo-alt"></i> Mis Direcciones
            </h2>
            <a href="<?= BASE_URL ?>index.php?controller=address&action=create"
               class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Nueva dirección
            </a>
        </div>
    </div>

    <?php if(empty($addresses)): ?>
        <div class="text-center py-5">
            <i class="bi bi-geo fs-1 text-muted"></i>
            <p class="text-muted mt-2">No tienes direcciones guardadas.</p>
        </div>
    <?php else: ?>
        <div class="row g-3">
            <?php foreach($addresses as $addr): ?>
            <div class="col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <h6 class="fw-bold">
                            <i class="bi bi-house"></i>
                            <?= htmlspecialchars($addr['direccion']) ?>
                        </h6>
                        <p class="text-muted small mb-1">
                            <?= htmlspecialchars($addr['barrio'] ?? '') ?>,
                            <?= htmlspecialchars($addr['ciudad']) ?>
                        </p>
                        <?php if(!empty($addr['referencia'])): ?>
                            <p class="text-muted small">
                                Ref: <?= htmlspecialchars($addr['referencia']) ?>
                            </p>
                        <?php endif; ?>
                    </div>
                    <div class="card-footer d-flex gap-2">
                        <a href="<?= BASE_URL ?>index.php?controller=address&action=edit&id=<?= $addr['id'] ?>"
                           class="btn btn-sm btn-outline-warning">
                            <i class="bi bi-pencil"></i> Editar
                        </a>
                        <a href="<?= BASE_URL ?>index.php?controller=address&action=delete&id=<?= $addr['id'] ?>"
                           class="btn btn-sm btn-outline-danger"
                           onclick="return confirm('¿Eliminar esta dirección?')">
                            <i class="bi bi-trash"></i>
                        </a>
                    </div>
                </div>
            </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>

</div>