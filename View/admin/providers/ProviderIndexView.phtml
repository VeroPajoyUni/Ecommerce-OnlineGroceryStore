<?php
/**
 * Vista Admin — Listado Proveedores
 * Ruta: View/admin/providers/ProviderIndexView.phtml
 */

?>

<div class="page-enter">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-truck"></i> Proveedores</h2>
        <a href="<?= BASE_URL ?>index.php?controller=provider&action=create"
           class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Nuevo Proveedor
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover admin-table">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Nombre</th>
                    <th>Teléfono</th>
                    <th>Dirección</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($providers as $prov): ?>
                <tr>
                    <td><?= $prov['id'] ?></td>
                    <td><strong><?= htmlspecialchars($prov['nombre']) ?></strong></td>
                    <td><?= htmlspecialchars($prov['telefono'] ?? '—') ?></td>
                    <td><?= htmlspecialchars($prov['direccion'] ?? '—') ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>index.php?controller=provider&action=edit&id=<?= $prov['id'] ?>"
                           class="btn btn-sm btn-outline-warning me-1">
                            <i class="bi bi-pencil">Editar</i>
                        </a>
                        <a href="<?= BASE_URL ?>index.php?controller=provider&action=delete&id=<?= $prov['id'] ?>"
                           class="btn btn-sm btn-outline-danger"
                           onclick="return confirm('¿Eliminar este proveedor?')">
                            <i class="bi bi-trash">Eliminar</i>
                        </a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>