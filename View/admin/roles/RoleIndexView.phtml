<?php
/**
 * Vista Admin — Listado Roles
 * Ruta: View/admin/roles/RoleIndexView.phtml
 */
?>

<div class="page-enter">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-shield-check"></i> Roles</h2>
        <a href="<?= BASE_URL ?>index.php?controller=role&action=create"
           class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Nuevo Rol
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover admin-table">
            <thead class="table-dark">
                <tr><th>#</th><th>Nombre</th><th>Acciones</th></tr>
            </thead>
            <tbody>
                <?php foreach($roles as $role): ?>
                <tr>
                    <td><?= $role['id'] ?></td>
                    <td><?= htmlspecialchars($role['nombre']) ?></td>
                    <td>
                        <a href="<?= BASE_URL ?>index.php?controller=role&action=edit&id=<?= $role['id'] ?>"
                           class="btn btn-sm btn-outline-warning me-1">
                            <i class="bi bi-pencil">Editar</i>
                        </a>
                        <a href="<?= BASE_URL ?>index.php?controller=role&action=delete&id=<?= $role['id'] ?>"
                           class="btn btn-sm btn-outline-danger"
                           onclick="return confirm('¿Eliminar este rol?')">
                            <i class="bi bi-trash">Eliminar</i>
                        </a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
