<?php
/**
 * Vista Admin — Listado de Inventario
 * Ruta: View/admin/inventory/InventoryIndexView.phtml
 */
?>

<div class="page-enter">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-box-seam-fill"></i> Inventario</h2>
        <a href="<?= BASE_URL ?>index.php?controller=inventory&action=create"
           class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Agregar Inventario
        </a>
    </div>

    <?php if(empty($inventories)): ?>
        <div class="text-center py-5">
            <i class="bi bi-inbox fs-1 text-muted"></i>
            <p class="text-muted mt-2">No hay registros de inventario.</p>
        </div>
    <?php else: ?>

    <div class="table-responsive">
        <table class="table table-hover admin-table align-middle">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Producto</th>
                    <th>Stock Actual</th>
                    <th>Stock Mínimo</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($inventories as $inv): ?>
                <tr>
                    <td><?= $inv['id'] ?></td>
                    <td><strong><?= htmlspecialchars($inv['producto'] ?? '') ?></strong></td>
                    <td><?= $inv['stock_actual'] ?? 0 ?></td>
                    <td><?= $inv['stock_minimo'] ?? 0 ?></td>
                    <td>
                        <?php
                            $stock = $inv['stock_actual'] ?? 0;
                            $minimo = $inv['stock_minimo'] ?? 0;
                            $clase = $stock <= $minimo ? 'bg-danger' : 'bg-success';
                            $estado = $stock <= $minimo ? 'Bajo' : 'Normal';
                        ?>
                        <span class="badge <?= $clase ?>">
                            <?= $estado ?>
                        </span>
                    </td>
                    <td>
                        <a href="<?= BASE_URL ?>index.php?controller=inventory&action=edit&id=<?= $inv['id'] ?>"
                           class="btn btn-sm btn-outline-warning me-1">
                            <i class="bi bi-pencil">Editar</i>
                        </a>
                        <a href="<?= BASE_URL ?>index.php?controller=inventory&action=delete&id=<?= $inv['id'] ?>"
                           class="btn btn-sm btn-outline-danger"
                           onclick="return confirm('¿Eliminar este registro?')">
                            <i class="bi bi-trash">Eliminar</i>
                        </a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>

    <?php endif; ?>
</div>
                <a href="https://facebook.com" target="_blank"><i class="fab fa-facebook"></i></a>
                <a href="https://instagram.com" target="_blank"><i class="fab fa-instagram"></i></a>
                <a href="https://wa.me/123456789" target="_blank"><i class="fab fa-whatsapp"></i></a>
            </div>
        </div>

    </div>

    <!-- FOOTER BOTTOM -->
    <div class="footer-bottom">
        © 2026 Tienda - Todos los derechos reservados
    </div>
</footer>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
