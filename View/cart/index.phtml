<?php
/**
 * Vista Carrito — AnimaMarket
 */
?>

<div class="page-enter">
    <div class="d-flex align-items-center gap-2 mb-4">
        <button class="btn-back" onclick="history.back()" title="Volver">
            <img src="<?= BASE_URL ?>icons/volver.png" alt="Volver" class="btn-back-icon"> Volver
        </button>
        <h1 class="mb-0"><i class="bi bi-cart3"></i> Tu Carrito</h1>
    </div>
</div>

<main>
<div class="row g-4">

    <!-- PRODUCTOS -->
    <div class="col-md-8">

        <div class="d-flex justify-content-between align-items-center mb-3">
            <h5>Tu Despensa</h5>
            <span class="badge bg-primary">
                <?= count($productosCarrito) ?> artículos
            </span>
        </div>

        <?php if(empty($productosCarrito)): ?>
            <div class="text-center py-5">
                <i class="bi bi-cart-x fs-1 text-muted"></i>
                <p class="text-muted mt-2">Tu carrito está vacío.</p>
                <a href="<?= BASE_URL ?>index.php?controller=product&action=index"
                   class="btn btn-primary mt-2">
                    Ver productos
                </a>
            </div>
        <?php endif; ?>

        <?php foreach($productosCarrito as $producto): ?>
        <div class="card mb-3 cart-item">
            <div class="card-body d-flex align-items-center gap-3">

                <img src="<?= !empty($producto['imagen'])
                    ? BASE_URL . $producto['imagen']
                    : BASE_URL . 'img/default.jpg' ?>"
                     class="cart-item-img rounded"
                     alt="<?= htmlspecialchars($producto['nombre']) ?>">

                <div class="flex-grow-1">
                    <h6 class="mb-0"><?= htmlspecialchars($producto['nombre']) ?></h6>
                    <small class="text-muted">
                        $<?= number_format($producto['precio'], 0, ',', '.') ?>
                        × <?= $producto['cantidad'] ?>
                    </small>
                </div>

                <strong>$<?= number_format($producto['subtotal'], 0, ',', '.') ?></strong>

                <form method="POST"
                      action="<?= BASE_URL ?>index.php?controller=cart&action=delete">
                    <input type="hidden" name="producto_id" value="<?= $producto['id'] ?>">
                    <button type="submit" class="btn btn-sm btn-outline-danger">
                        <i class="bi bi-trash"></i>
                    </button>
                </form>

            </div>
        </div>
        <?php endforeach; ?>

    </div>

    <!-- RESUMEN -->
    <div class="col-md-4">
        <div class="card cart-summary sticky-top">
            <div class="card-body">

                <h5 class="card-title">Resumen</h5>
                <hr>

                <div class="d-flex justify-content-between mb-2">
                    <span>Subtotal</span>
                    <strong>$<?= number_format($subtotal, 0, ',', '.') ?></strong>
                </div>
                <div class="d-flex justify-content-between mb-3 text-muted small">
                    <span>Envío</span>
                    <span>Calculado al finalizar</span>
                </div>
                <hr>
                <div class="d-flex justify-content-between mb-4">
                    <strong>Total</strong>
                    <strong class="text-primary fs-5">
                        $<?= number_format($subtotal, 0, ',', '.') ?>
                    </strong>
                </div>

                <?php if(!empty($productosCarrito)): ?>
                    <?php if(isLoggedIn()): ?>
                        <form method="POST"
                              action="<?= BASE_URL ?>index.php?controller=order&action=checkout">
                            <button type="submit" class="btn btn-success w-100 btn-lg">
                                <i class="bi bi-bag-check"></i> Finalizar compra
                            </button>
                        </form>
                    <?php else: ?>
                        <a href="<?= BASE_URL ?>index.php?controller=Auth&action=login"
                           class="btn btn-primary w-100 btn-lg">
                            <i class="bi bi-person"></i> Ingresar para comprar
                        </a>
                    <?php endif; ?>
                <?php endif; ?>

            </div>
        </div>
    </div>

</div>
</main>